type User {
    id: ID!
    name: String!
    email: String!
}

type Query {
    users: [User!] @paginate
    user(id: ID! @eq): User @find
}

type Mutation {
    createUser(name: String!, email: String!): User! @create
}
